apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins

helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    version: 5.9.38
    releaseName: jenkins
    namespace: jenkins
    valuesFile: values.yaml

resources:
  - namespace.yaml